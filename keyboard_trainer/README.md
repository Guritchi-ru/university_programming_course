[CMakeLists.txt](./CMakeLists.txt)
для сборки ввести

```bash
mkdir build && cd build
cmake ..
make
```

Создайте файл config.cfg в папке build

[config.cfg](./config.cfg) - пример файла конфигурации

Для запуска ввести
```bash
./paint
```
Для запуска тестов ввести
```bash
ctest
```